{
    "version": "2.0.0",
    "tasks": [
        // Disabled automatic deploy task to prevent accidental auto-commits.
        // To re-enable, restore the original task and the auto-deploy.sh script.
    ]
}

